package com.self.ecom
/*

 if after adding firestore app crashes ithout any reason, check expiry

 firrstore db >  rules>  change year > publish
   allow read, write: if request.time < timestamp.date(2035, 7, 18);

 */
